import 'form_login.dart';
import 'form_register.dart';

/// [Forms] is a class that contains all the app forms
class Forms {
  static FormLogin get login => FormLogin();
  static FormRegister get register => FormRegister();
}
